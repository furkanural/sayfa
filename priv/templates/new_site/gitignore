# Build artifact — regenerated by `mix sayfa.build`.
# CI/CD builds the site fresh on deploy, so this doesn't need to be committed.
/output/

# Dependencies
/deps/
/_build/

# IDE
.elixir_ls/
.vscode/

# OS
.DS_Store
Thumbs.db
