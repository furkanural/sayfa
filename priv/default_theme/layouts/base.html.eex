<!DOCTYPE html>
<html lang="<%= Map.get(assigns, :lang, "en") %>" dir="<%= Map.get(assigns, :dir, "ltr") %>" class="scroll-smooth">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= if assigns[:page_title], do: "#{assigns[:page_title]} — #{@site.title}", else: @site.title %></title>
  <%= Sayfa.SEO.favicon_tags() %>
  <%= Sayfa.SEO.meta_tags(assigns[:content], @site) %>
  <%= Sayfa.SEO.json_ld(assigns[:content], @site) %>
  <%= Sayfa.SEO.hreflang_tags(assigns[:content], @site, assigns[:archive_alternates]) %>
  <link rel="alternate" type="application/atom+xml" title="<%= @site.title %>" href="<%= String.trim_trailing(@site.base_url, "/") %>/feed.xml">
  <link rel="alternate" type="application/feed+json" title="<%= @site.title %>" href="<%= String.trim_trailing(@site.base_url, "/") %>/feed.json">
  <link rel="stylesheet" href="/assets/css/main.css">
</head>
<body class="bg-white dark:bg-slate-900 text-slate-900 dark:text-slate-100 font-sans antialiased">
  <%= @block.(:header, nav: Map.get(@site, :nav, []), page_url: assigns[:page_url]) %>
  <%= @inner_content %>
  <%= @block.(:footer, author: Map.get(@site, :author, @site.title)) %>
  <%= @block.(:code_copy, []) %>
  <script src="/assets/js/enhancements.js" defer></script>
  <%= @block.(:analytics, []) %>
</body>
<!-- Built with Sayfa https://hex.pm/packages/sayfa -->
</html>
